# etl simulation using python using the file handling techniques
we have 3 files here , one file for the actual data we used as input data where we have dirty datas and we have another file for storing the output cleaned data.

